Schema::create('{{model}}', function(Blueprint $table) {
            $table->id();
            {{schema_up}}
            $table->timestamps();
        });
        DB::statement("ALTER TABLE {{model}} comment ''");